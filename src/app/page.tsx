// This file is intentionally blank.
// The root route is handled by the (auth) route group in src/app/(auth)/page.tsx
// or redirects to /dashboard if the user is authenticated.
export default function MutedRootPage() {
  return null;
}
